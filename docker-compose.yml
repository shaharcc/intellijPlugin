version: '3.8'

services:
  analyzer-service:
    build:
      context: ./analyzerService
      dockerfile: Dockerfile
    ports:
      - "6080:8080"
    volumes:
      - ./analyzerService:/app/analyzerService  # Adjusted mount point if necessary
    command: ["./gradlew", "bootRun"]
    working_dir: /app/analyzerService  # Adjusted working directory

  intellij:
    build:
      context: .
      dockerfile: Dockerfile.intellij
    ports:
      - "6080:80"  # Port for VNC access
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./example:/root/example
    environment:
      - DISPLAY=unix$DISPLAY
    tty: true
    stdin_open: true
